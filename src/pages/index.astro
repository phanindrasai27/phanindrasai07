---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <div class="container">
    <h1>PHANINDRA SAI BOLLARAPU</h1>
    
    <p class="intro">Versatile AI Engineer & Generalist | AI, Data & Strategy for Product Innovation | Philosophy for overthinking!</p>

    <div class="links">
      <a href="https://github.com/phanindrasai27">github</a>, 
      <a href="https://linkedin.com/in/phanindra-sai">linkedin</a>, 
      <a href="https://x.com/phanindrasai07">twitter</a>, 
      <a href="https://medium.com/@phanindra_sai27">medium</a>, 
      <a href="https://looper27.bearblog.dev">blog</a>, 
      <a href="mailto:bollarapuphanindra1375645@gmail.com">email</a>,
      <a href="https://phanindrasai.notion.site">product portfolio</a>
    </div>
  </div>
</Layout>

<style>
  .container {
    display: flex;
    flex-direction: column;
    gap: clamp(3rem, 8vh, 5rem);
    margin-top: clamp(2rem, 5vh, 4rem);
    padding: 0 clamp(1rem, 3vw, 2rem);
  }

  h1 {
    font-size: clamp(2.5rem, 8vw, 4rem);
    margin: 0;
    line-height: 1.1;
    letter-spacing: -0.04em;
    max-width: 15ch;
  }

  .intro {
    font-size: clamp(1.25rem, 3vw, 1.5rem);
    margin: 0;
    opacity: 0.8;
    max-width: 40ch;
    line-height: 1.4;
  }

  .links {
    font-size: clamp(1.5rem, 4vw, 2rem);
    margin-top: clamp(1rem, 3vh, 2rem);
    line-height: 1.6;
  }

  .links a {
    color: rgba(255, 255, 255, 0.6);
    text-decoration: none;
    transition: all 0.3s ease;
    white-space: nowrap;
    transform-origin: 0 50%;
  }

  .links a:hover {
    color: rgba(255, 255, 255, 0.9);
  }

  .links a:active {
    transform: scale(0.95);
    transition: transform 0.1s ease;
  }

  @media (max-width: 768px) {
    .container {
      gap: 3rem;
    }
  }

  @media (max-width: 480px) {
    .container {
      gap: 2.5rem;
    }

    .links {
      line-height: 2;
    }
  }

  @media (min-width: 2000px) {
    .container {
      gap: 6rem;
    }
  }
</style>